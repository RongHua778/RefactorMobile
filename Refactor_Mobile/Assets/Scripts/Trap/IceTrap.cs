using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IceTrap : TrapContent
{
    //public override void OnContentPass(Enemy enemy, GameTileContent content = null)
    //{
        //base.OnContentPass(enemy);
        //float slowRate = enemy.PassedTraps.Count * 0.5f * TrapIntensify;
        //BuffInfo info = new BuffInfo(EnemyBuffName.SlowDown, slowRate,0, 2f);
        //enemy.Buffable.AddBuff(info);
    //}

}